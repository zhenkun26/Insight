name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: python -m pip install --upgrade pip && pip install ".[dev]"
      - name: Ruff check
        run: ruff check app tests scripts
      - name: Import check
        run: python -c "from app.main import app; from app.services.jobs import IndexJobService; from app.workflows import WorkflowState; print(app.title, WorkflowState.__name__, IndexJobService.__name__)"
      - name: Tests
        run: pytest
      - name: Evaluation smoke test
        run: python scripts/evaluate.py --output /tmp/insight-evaluation.json
